<!DOCTYPE html>
<html>
  <head>
    <title>마이페이지</title>
    <script>
      // 스크롤 이벤트 핸들러
      window.onscroll = function () {
        var topbar = document.querySelector('.topbar');
        var sidenav = document.querySelector('.sidenav');
        if (window.scrollY > 0) {
          topbar.style.top = '0';
          sidenav.style.top = '60px';
        } else {
          topbar.style.top = '-60px';
          sidenav.style.top = '0';
        }
      };

      //로그아웃 기능 구현
      function logout() {
        alert('로그아웃 되었습니다.');
        window.location.href = '/logout';
      }
    </script>
    <style>
      /*배경 이미지 설정*/
      body {
        background-color: #fff;
      }

      .background-image {
        background-image: url('/univ');
        background-size: cover;
        background-position: center;
        background-attachment: fixed;
        background-repeat: no-repeat;
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        opacity: 0.5;
      }

      /*흰색 파트*/
      .white-rectangle {
        width: 84.2%;
        height: 81.5%;
        background-color: beige;
        position: absolute;
        top: 13.5%;
        left: 10.5%;
        opacity: 0.65;
      }

      .profile-section {
        text-align: left;
        background-color: rgb(255, 255, 255, 0, 0.7);
        padding: 20px;
        border-radius: 10px;
        position: absolute;
        top: 15%;
        left: 13.5%;
        width: 350px;
        height: 100px;
      }

      h1 {
        font-weight: bold;
      }

      label {
        display: block;
        margin-top: 10px;
        font-weight: bold;
        font-size: 18px;
      }

      input[type='text'],
      input[type='password'],
      select {
        width: 100%;
        padding: 10px;
        margin: 5px 0;
        border-radius: 1px;
        box-sizing: border-box;
      }

      button {
        background-color: rgb(76, 47, 189);
        color: white;
        padding: 10px 20px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
      }

      /* 초기 스타일 */
      .topbar {
        height: 80px;
        background-color: #222;
        color: #fff;
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 0 20px;
        position: fixed;
        top: 0;
        width: 92%;
        transition: top 0.3s;
        font-size: 20px; /* 텍스트 크기 고정 */
      }

      /* 상단 바 링크 스타일 */
      .topbar a {
        padding: 0px 5px;
        text-decoration: none;
        color: #fff;
        display: block;
        margin-right: 10px;
        font-size: 30px;
      }

      /* 메뉴 링크 스타일 (마우스 호버 시) */
      .topbar a:hover {
        background-color: #444;
      }

      /* 로고 이미지 스타일 */
      .logo {
        max-height: 65px; /* 최대 높이 설정 */
      }

      /* 로그인 정보 스타일 */
      .login-info {
        color: #fff;
        display: flex;
        align-items: center;
      }

      .username {
        margin-right: 10px;
      }

      /* 로그아웃 버튼 스타일 */
      .logout-button {
        background-color: rgb(76, 47, 189);
        color: #fff;
        padding: 5px 10px;
        border: 1;
        cursor: pointer;
        margin-right: 10px;
        position: relative;
        right: 2%;
        font-size: 15px;
      }

      /* 사이드 메뉴 바 스타일 */
      .sidenav {
        height: 80%;
        width: 170px;
        position: fixed;
        top: 110px;
        left: 20;
        background-color: #333;
        padding-top: 20px;
      }

      /* 메뉴 링크 스타일 */
      .sidenav a {
        padding: 15px 25px;
        text-decoration: none;
        font-size: 18px;
        color: #fff;
        display: block;
      }

      /* 메뉴 링크 스타일 (마우스 호버 시) */
      .sidenav a:hover {
        background-color: #444;
      }
    </style>
  </head>
  <body>
    
    <!-- 배경 이미지-->
    <div class="background-image"></div>
    <div class="white-rectangle"></div>

    <!-- 상단 바 -->
    <div class="topbar">
      <a href="/main">
        <img class="logo" src="/logo" alt="로고 이미지" /></a>
      <a href="/upload"><b>설문 등록</b></a>
      <a href="/mypage"><b>마이페이지</b></a>
      <div class="login-info">
        <div class="username"><%= data.name %>님 환영합니다.</div>
        <div class="logout-button" onclick="logout()">로그아웃</div>
      </div>
    </div>

    <!-- 사이드 메뉴 바 -->
    <div class="sidenav">
      <a href="/mypage"><b>마이페이지</b></a>
      <a href="/my_updateinfo"><b>회원정보 수정</b></a>
      <a href="/my_updatePW"><b>비밀번호 수정</b></a>
      <a href="/my_survey_list"><b>내 설문 목록</b></a>
    </div>

    <!-- 메인 컨텐츠 -->
    <form action="/my_updatePW" id="updatePW" method="POST">
        <div class="profile-section">
        <h1>비밀번호 수정</h1>
  
        <label for="updatePassword">현재 비밀번호:</label>
        <input type="password" id="currentPassword" name="currentPassword" placeholder="현재 비밀번호 입력" required>
        
        <label for="updatePassword">변경 비밀번호:</label>
        <input type="password" id="updatePassword" name="updatePassword" placeholder="변경할 비밀번호 입력" required>
        
        <label for="confirmPassword">비밀번호 재확인:</label>
        <input type="password" id="confirmPassword" name="confirmPassword" placeholder="재확인 비밀번호 입력" required>
  
        <button type="submit" id="send">저장</button>
      </form>

      
  </body>
</html>